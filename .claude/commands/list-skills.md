List all available project slash commands.

Glob for `.claude/commands/**/*.md` and display each command name (derived from filename) with a one-line description.

For the description, read the first line of each file (which is typically a short summary).

Format as a table with columns: Command, Description.

Sort alphabetically by command name.
